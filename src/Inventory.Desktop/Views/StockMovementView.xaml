<UserControl x:Class="Inventory.Desktop.Views.StockMovementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:enums="clr-namespace:Inventory.Desktop.Enums;assembly=Inventory.Desktop"
             xmlns:converters="clr-namespace:Inventory.Desktop.Converters"
             xmlns:translation="clr-namespace:Inventory.Desktop.Converters">
    <UserControl.Resources>
        <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <converters:UtcToLocalDateConverter x:Key="UtcToLocalDateConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="290"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Movement Form -->
        <GroupBox Grid.Column="0" Margin="0,0,10,0">
            <GroupBox.Header>
                <translation:TranslationExtension Key="stockMovements.title"/>
            </GroupBox.Header>
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Margin="0,0,0,5">
                    <TextBlock.Inlines>
                        <Run>
                            <Run.Text>
                                <translation:TranslationExtension Key="stockMovements.productLabel"/>
                            </Run.Text>
                        </Run>
                        <Run Text=" *"/>
                    </TextBlock.Inlines>
                </TextBlock>
                <ComboBox Grid.Row="1" 
                         ItemsSource="{Binding Products}"
                         SelectedItem="{Binding SelectedProduct}"
                         Margin="0,0,0,10">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1}) - Qty: {2}">
                                        <Binding Path="Name"/>
                                        <Binding Path="SKU"/>
                                        <Binding Path="Quantity"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Grid.Row="2" Margin="0,0,0,5">
                    <TextBlock.Inlines>
                        <Run>
                            <Run.Text>
                                <translation:TranslationExtension Key="stockMovements.movementTypeLabel"/>
                            </Run.Text>
                        </Run>
                        <Run Text=" *"/>
                    </TextBlock.Inlines>
                </TextBlock>
                <ComboBox Grid.Row="3" 
                         ItemsSource="{Binding MovementTypes}"
                         SelectedItem="{Binding SelectedMovementTypeItem}"
                         DisplayMemberPath="DisplayName"
                         Margin="0,0,0,10"/>
                <TextBlock Grid.Row="4" 
                         FontSize="10" 
                         Foreground="Gray" 
                         TextWrapping="Wrap"
                         Margin="0,0,0,10">
                    <TextBlock.Text>
                        <translation:TranslationExtension Key="stockMovements.movementTypesHelp"/>
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Grid.Row="5" Margin="0,0,0,5">
                    <TextBlock.Inlines>
                        <Run>
                            <Run.Text>
                                <translation:TranslationExtension Key="stockMovements.quantityLabel"/>
                            </Run.Text>
                        </Run>
                        <Run Text=" *"/>
                    </TextBlock.Inlines>
                </TextBlock>
                <TextBox Grid.Row="6" Text="{Binding Quantity}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="7" Margin="0,0,0,5">
                    <TextBlock.Text>
                        <translation:TranslationExtension Key="stockMovements.reasonLabel"/>
                    </TextBlock.Text>
                </TextBlock>
                <TextBox Grid.Row="8" Text="{Binding Reason}" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         Height="50"
                         Margin="0,0,0,10"/>

                <TextBlock Grid.Row="9" Margin="0,0,0,5">
                    <TextBlock.Text>
                        <translation:TranslationExtension Key="stockMovements.notesLabel"/>
                    </TextBlock.Text>
                </TextBlock>
                <TextBox Grid.Row="10" Text="{Binding Notes}" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         Height="60"
                         Margin="0,0,0,10"/>

                <Button Grid.Row="11" 
                       Command="{Binding ExecuteMovementCommand}"
                       Height="35"
                       FontSize="14"
                       FontWeight="Bold">
                    <Button.Content>
                        <translation:TranslationExtension Key="stockMovements.execute"/>
                    </Button.Content>
                </Button>
            </Grid>
        </GroupBox>

        <!-- Movements History -->
        <GroupBox Grid.Column="1">
            <GroupBox.Header>
                <translation:TranslationExtension Key="stockMovements.history"/>
            </GroupBox.Header>
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" 
                       Command="{Binding RefreshCommand}"
                       HorizontalAlignment="Right"
                       Margin="0,0,0,5"
                       Padding="10,5">
                    <Button.Content>
                        <translation:TranslationExtension Key="common.refresh"/>
                    </Button.Content>
                </Button>

                <DataGrid Grid.Row="1" 
                         ItemsSource="{Binding Movements}" 
                         SelectedItem="{Binding SelectedMovement}"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         GridLinesVisibility="All"
                         HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="150">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.date"/>
                            </DataGridTextColumn.Header>
                            <DataGridTextColumn.Binding>
                                  <Binding Path="CreatedAt" Converter="{StaticResource UtcToLocalDateConverter}"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding ProductName}" Width="*">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.product"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding MovementType}" Width="100">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.type"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Quantity}" Width="80">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.quantity"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding CreatedBy}" Width="120">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.createdBy"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Reason}" Width="150">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.reason"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Notes}" Width="*">
                            <DataGridTextColumn.Header>
                                <translation:TranslationExtension Key="stockMovements.notes"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- Pagination Controls -->
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.Visibility>
                        <MultiBinding Converter="{StaticResource CountToVisibilityConverter}">
                            <Binding Path="TotalCount"/>
                            <Binding Path="PageSize"/>
                        </MultiBinding>
                    </Grid.Visibility>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding PaginationInfo}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0">
                            <TextBlock.Inlines>
                                <Run>
                                    <Run.Text>
                                        <translation:TranslationExtension Key="pagination.pageSize"/>
                                    </Run.Text>
                                </Run>
                                <Run Text=":"/>
                            </TextBlock.Inlines>
                        </TextBlock>
                        <ComboBox SelectedValue="{Binding PageSize, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Width="100" VerticalContentAlignment="Center">
                            <ComboBox.Items>
                                <ComboBoxItem Content="10" Tag="10"/>
                                <ComboBoxItem Content="25" Tag="25"/>
                                <ComboBoxItem Content="50" Tag="50"/>
                                <ComboBoxItem Content="100" Tag="100"/>
                            </ComboBox.Items>
                        </ComboBox>
                        <Button Command="{Binding ChangePageSizeCommand}" 
                                Margin="5,0,0,0" Padding="10,5">
                            <Button.Content>
                                <translation:TranslationExtension Key="pagination.apply"/>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Command="{Binding FirstPageCommand}" 
                                Margin="0,0,5,0" Padding="10,5" IsEnabled="{Binding HasPreviousPage}">
                            <Button.Content>
                                <translation:TranslationExtension Key="pagination.first"/>
                            </Button.Content>
                        </Button>
                        <Button Command="{Binding PreviousPageCommand}" 
                                Margin="0,0,5,0" Padding="10,5" IsEnabled="{Binding HasPreviousPage}">
                            <Button.Content>
                                <translation:TranslationExtension Key="pagination.previous"/>
                            </Button.Content>
                        </Button>
                        <TextBlock VerticalAlignment="Center" Margin="5,0">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0} / {1}">
                                    <Binding Path="CurrentPage"/>
                                    <Binding Path="TotalPages"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <Button Command="{Binding NextPageCommand}" 
                                Margin="5,0,5,0" Padding="10,5" IsEnabled="{Binding HasNextPage}">
                            <Button.Content>
                                <translation:TranslationExtension Key="pagination.next"/>
                            </Button.Content>
                        </Button>
                        <Button Command="{Binding LastPageCommand}" 
                                Padding="10,5" IsEnabled="{Binding HasNextPage}">
                            <Button.Content>
                                <translation:TranslationExtension Key="pagination.last"/>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>

